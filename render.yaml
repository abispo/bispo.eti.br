services:
  - type: web
    name: bispo-portfolio
    runtime: static
    buildCommand: npm run build
    staticPublishPath: ./out
    pullRequestPreviewsEnabled: true
    envVars:
      - key: NODE_ENV
        value: production
